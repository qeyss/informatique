Function CreaFicRep {

PARAM(
	[validatecount(1,10)][string]$Path=$(throw{"Chemin Non rempli, merci de corriger cela"}),
	[string[]]$Rep,
	[string[]]$Fic	
)
foreach ($Elem1 in $Rep)
{
	ni $path\$Elem1 -itemtype "Directory"
}
foreach ($Elem2 in $Fic)
{
	ni $path\$Elem2 -itemtype "File"
}
}
